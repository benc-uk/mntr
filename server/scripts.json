{
  "$schema": "https://deno.land/x/denon@2.4.5/schema.json",
  "watcher": {
    "match": ["**/*.*"]
  },
  "scripts": {
    "start": {
      "cmd": "deno run --allow-net --allow-env --allow-read --allow-write server.ts",
      "desc": "run mntr server"
    }
  }
}
